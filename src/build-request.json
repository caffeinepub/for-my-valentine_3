{
  "kind": "build_request",
  "title": "Update Final Step NO button: stop moving on hover and show new message on click",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-44",
      "text": "In the Valentine proposal flow Final step (Page 5), remove the NO button “run away/move on hover” behavior so that hovering the NO button does not change its position and it remains in the normal button layout.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages"
        ],
        "quotes": [
          "remove the run feature of no button"
        ]
      },
      "acceptanceCriteria": [
        "Hovering the NO button does not move it to a random position.",
        "The NO button remains positioned normally within the YES/NO button row (no absolute positioning driven by hover).",
        "No hover event handler is attached that changes NO button coordinates/state."
      ]
    },
    {
      "id": "REQ-45",
      "text": "In the Valentine proposal flow Final step (Page 5), update the NO button click behavior so it displays exactly this response text: \"The button is broken, try the green one instead 😏💚\".",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages"
        ],
        "quotes": [
          "when no button is clicked show that The button is broken, try the green one instead 😏💚"
        ]
      },
      "acceptanceCriteria": [
        "Clicking the NO button sets the response message to exactly: \"The button is broken, try the green one instead 😏💚\" (including emoji and punctuation).",
        "The response message renders in the existing response area with the current styling (no new UI component required).",
        "The YES button behavior remains unchanged: it still triggers confetti and shows the existing YES response text."
      ]
    },
    {
      "id": "REQ-46",
      "text": "Update `frontend/DEPLOYMENT_CHECKLIST.md` to reflect the new NO button behavior (no movement on hover; on click it shows the updated message).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages"
        ],
        "quotes": [
          "remove the run feature of no button and when no button is clicked show that The button is broken, try the green one instead 😏💚"
        ]
      },
      "acceptanceCriteria": [
        "The checklist no longer claims the NO button moves on hover.",
        "The checklist includes verification that clicking NO shows: \"The button is broken, try the green one instead 😏💚\"."
      ]
    }
  ],
  "constraints": [
    "Frontend must remain a single-page in-app state flow (no full page reloads).",
    "Do not modify files under `frontend/src/components/ui` (compose them as-is).",
    "Keep the existing romantic theme and layout; only change the NO button behavior and related copy.",
    "Backend remains unchanged (no new Motoko canister logic required)."
  ],
  "nonGoals": [
    "Do not change the YES button label, color, confetti animation, or YES response text.",
    "Do not change headings/copy in other steps of the flow.",
    "Do not add new persistence, storage, or backend APIs for this change."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}